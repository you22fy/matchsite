<%= stylesheet_link_tag 'index_rooms', :media => "all" %>
<div class="list">
  <% @anotherEntries.each do |e| %>
      <%= link_to room_path(e.room.id) do %>
        <%= e.user.name %>とのDM</br>
      <% end %>
  <% end %>
</div>